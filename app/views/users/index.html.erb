<h3>スケジュール一覧</h3>

<% if flash[:notice] %>
<p class="notice"><%= flash[:notice] %></p>
<% end %>

 <table>
   <thead>
     <tr>
       <th>id</th>
       <th>タイトル</th>
       <th>開始日</th>
       <th>終了日</th>
       <th>終日</th>
       <th>スケジュールメモ</th>
       <th>更新日時</th>
       <th>作成日時</th>
     </tr>
   </thead>
 
   <tbody>
     <% @users.each do |user| %>
       <tr>
         <td><%= user.id %></td>
         <td><%= user.title %></td>
         <td><%= user.start_day %></td>
         <td><%= user.end_day %></td>
         <td>
          <% if user.all_day == true %>
          <%= "○" %>
          <% else user.all_day == false %>
          <% end %></td>
        </td>
         <td><%= user.memo %></td>
         <td><%= user.updated_at.to_s(:datetime_jp) %></td>
         <td><%= user.created_at.to_s(:datetime_jp) %></td>
         
         <td>
         <%= link_to "詳細", user %>
         </td>
         <td>
          <%= link_to "編集", [:edit, user] %>
         </td>
         <td>
           <%= link_to "削除", user, method: :delete, data: { confirm: "本当に削除しますか？" } %>
         </td>
         </tr>
     <% end %>
   </tbody>
 </table>
 
 <p><%= link_to "予定の新規登録", :new_user %></p>

 <div class ="information">
        <a class = "information-1"><%= "合計予定数：#{@users.count}件" %></a>
        <a class ="information-2"><%= "本日の日付：#{Date.today}" %></a>
    </div>